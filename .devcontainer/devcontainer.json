{
  "build": {
    "dockerfile": "Dockerfile"
  },
  "privileged": true,
  "forwardPorts": [8444],
  "portsAttributes": {
    "8444": {
      "label": "Cinnamon Desktop (KasmVNC)",
      "onAutoForward": "openBrowser",
      "visibility": "public"
    }
  },
  "runArgs": ["--cap-add=SYS_ADMIN"],
  "postCreateCommand": "mkdir -p ~/.vnc && echo -e \"qwerty\\nqwerty\" | vncpasswd -u qwerty -w -o ~/.kasmpasswd && chmod 600 ~/.kasmpasswd && touch ~/.vnc/.de-was-selected && echo -e '#!/bin/sh\\nexec cinnamon-session' > ~/.vnc/xstartup && chmod +x ~/.vnc/xstartup",
  "postStartCommand": "vncserver :1"
}